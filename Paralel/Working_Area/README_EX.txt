You should chouse the execution.sh in the makefile, exe section, if you want to run the program in your own computer. On the other hand, if you choose the cluster, you should use execution_cluster.sh. Maybe you need to use:
chmod a+rwx *.sh
